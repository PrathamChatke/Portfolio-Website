export default function CloudArchitecture() {
  return (
    <section id="cloudarchitecture" className="p-10 text-center">
      <h2 className="text-3xl font-bold">Cloud Architecture</h2>
      <p className="text-gray-400 mt-4">
        GitHub → Jenkins → Docker → AWS EC2 → Nginx
      </p>
    </section>
  );
}